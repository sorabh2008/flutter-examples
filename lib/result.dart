import 'package:flutter/widgets.dart';

class Result extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("Done and dusted.")],
    );
  }
}
